{
  "slug": "callrail",
  "name": "CallRail",
  "description": "CallRail call tracking and analytics platform. List and retrieve calls, manage companies, and view tracking number configurations.",
  "logo": "https://www.google.com/s2/favicons?domain=callrail.com&sz=128",
  "categories": [
    "call-tracking",
    "analytics",
    "phone",
    "marketing",
    "leads"
  ],
  "base_url": "https://api.callrail.com/v3",
  "auth": {
    "types": [
      "bearer"
    ],
    "headers": {
      "Authorization": "{{token}}"
    },
    "credential_fields": [
      {
        "name": "token",
        "label": "Api Key",
        "description": "CallRail API key (generate at app.callrail.com/api)"
      },
      {
        "name": "accountId",
        "label": "Account Id",
        "description": "CallRail account ID"
      }
    ]
  },
  "tools": [
    {
      "name": "list_calls",
      "description": "Retrieve a paginated list of calls for a specific account. Supports filtering by date range, tags, and other criteria. Returns call details including caller info, duration, recording URL, source, and tracking number. Supports relative pagination for large result sets.",
      "method": "GET",
      "path": "/list-calls",
      "input_schema": {
        "type": "object",
        "properties": {
          "account_id": {
            "type": "string",
            "description": "The CallRail account ID."
          },
          "start_date": {
            "type": "string",
            "description": "Filter calls starting from this date (YYYY-MM-DD format)."
          },
          "end_date": {
            "type": "string",
            "description": "Filter calls up to this date (YYYY-MM-DD format)."
          },
          "fields": {
            "type": "string",
            "description": "Comma-separated list of additional fields to include (e.g. 'recording,duration,sentiment,keywords')."
          },
          "sort": {
            "type": "string",
            "description": "Field to sort results by (e.g. 'start_time', 'duration', 'customer_name')."
          },
          "order": {
            "type": "string",
            "description": "Sort order: 'asc' or 'desc'.",
            "enum": [
              "asc",
              "desc"
            ]
          },
          "per_page": {
            "type": "integer",
            "description": "Number of results per page. Default is 25, max is 250.",
            "default": 25
          },
          "page": {
            "type": "integer",
            "description": "Page number for pagination.",
            "default": 1
          },
          "tags": {
            "type": "string",
            "description": "Filter by tag name. Use comma-separated values for multiple tags."
          },
          "search": {
            "type": "string",
            "description": "Search across caller name, phone number, and other fields."
          }
        },
        "required": [
          "account_id"
        ]
      }
    },
    {
      "name": "get_call",
      "description": "Retrieve detailed information about a specific call by its ID. Returns full call details including caller information, duration, recording URL, transcription, tags, sentiment analysis, and tracking source.",
      "method": "GET",
      "path": "/get-call",
      "input_schema": {
        "type": "object",
        "properties": {
          "account_id": {
            "type": "string",
            "description": "The CallRail account ID."
          },
          "call_id": {
            "type": "string",
            "description": "The unique identifier of the call to retrieve."
          },
          "fields": {
            "type": "string",
            "description": "Comma-separated list of additional fields to include (e.g. 'recording,duration,sentiment,transcription,keywords')."
          }
        },
        "required": [
          "account_id",
          "call_id"
        ]
      }
    },
    {
      "name": "list_companies",
      "description": "Retrieve a list of all companies associated with the CallRail account. Returns company IDs, names, time zones, and configuration details. Company IDs are required for filtering calls and trackers.",
      "method": "GET",
      "path": "/list-companies",
      "input_schema": {
        "type": "object",
        "properties": {
          "account_id": {
            "type": "string",
            "description": "The CallRail account ID."
          },
          "page": {
            "type": "integer",
            "description": "Page number for pagination.",
            "default": 1
          },
          "per_page": {
            "type": "integer",
            "description": "Number of results per page. Default is 25, max is 250.",
            "default": 25
          }
        },
        "required": [
          "account_id"
        ]
      }
    },
    {
      "name": "list_trackers",
      "description": "Retrieve a list of all call tracking numbers (trackers) for a specific account. Returns tracker IDs, names, tracking numbers, type (source or session), associated company, and destination numbers. Use company_id to filter trackers by company.",
      "method": "GET",
      "path": "/list-trackers",
      "input_schema": {
        "type": "object",
        "properties": {
          "account_id": {
            "type": "string",
            "description": "The CallRail account ID."
          },
          "company_id": {
            "type": "string",
            "description": "Filter trackers by company ID."
          },
          "page": {
            "type": "integer",
            "description": "Page number for pagination.",
            "default": 1
          },
          "per_page": {
            "type": "integer",
            "description": "Number of results per page. Default is 25, max is 250.",
            "default": 25
          },
          "sort": {
            "type": "string",
            "description": "Field to sort results by (e.g. 'name')."
          },
          "order": {
            "type": "string",
            "description": "Sort order: 'asc' or 'desc'.",
            "enum": [
              "asc",
              "desc"
            ]
          }
        },
        "required": [
          "account_id"
        ]
      }
    }
  ]
}