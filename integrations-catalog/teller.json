{
  "slug": "teller",
  "name": "Teller",
  "description": "MCP server for Teller - the API for US bank accounts. Fetch accounts, balances, transactions, account/routing numbers, identity, and beta payments. Self-serve signup with a free dev tier (100 live connections + unlimited sandbox). Teller development and production require a mutual-TLS client certificate; provide the certificate and private key fields for live access.",
  "logo": "https://www.google.com/s2/favicons?domain=teller.io&sz=128",
  "categories": [
    "banking",
    "open-banking",
    "transactions",
    "accounts",
    "us",
    "teller"
  ],
  "base_url": "https://api.teller.io",
  "auth": {
    "types": [
      "basic"
    ],
    "headers": {
      "Authorization": "Basic {{basic_auth}}",
      "Accept": "application/json"
    },
    "mtls": {
      "cert_field": "client_certificate_pem",
      "key_field": "client_private_key_pem"
    },
    "credential_fields": [
      {
        "name": "username",
        "label": "Access Token",
        "description": "Teller access_token returned by Teller Connect (token_xxx). Sent as the username in HTTP Basic auth.",
        "required": true,
        "type": "password"
      },
      {
        "name": "password",
        "label": "Password",
        "description": "Teller uses Basic auth with the access token as the username and ignores the password. Enter the literal value 'x' \u2014 the runtime needs a non-empty password to build the Basic header.",
        "required": true,
        "type": "password"
      },
      {
        "name": "client_certificate_pem",
        "label": "Client Certificate PEM",
        "description": "Teller mTLS client certificate PEM. Required for development and production access; optional in sandbox.",
        "required": false,
        "type": "password"
      },
      {
        "name": "client_private_key_pem",
        "label": "Client Private Key PEM",
        "description": "Private key PEM for the Teller mTLS client certificate. Required for development and production access; optional in sandbox.",
        "required": false,
        "type": "password"
      }
    ]
  },
  "tools": [
    {
      "name": "list_accounts",
      "description": "List all bank accounts linked to the access token. Returns account id, name, type, subtype, last-four, currency, and institution.",
      "method": "GET",
      "path": "/accounts",
      "input_schema": {
        "type": "object",
        "properties": {}
      }
    },
    {
      "name": "get_account",
      "description": "Get a single linked bank account by account id. Inspect links.payments: absent means this account cannot originate payments. Where present, OPTIONS on that URL reports supported payment schemes; Teller payments are beta Zelle, not general-purpose bank transfers.",
      "method": "GET",
      "path": "/accounts/{account_id}",
      "input_schema": {
        "type": "object",
        "properties": {
          "account_id": {
            "type": "string",
            "description": "The account id from list_accounts"
          }
        },
        "required": [
          "account_id"
        ]
      }
    },
    {
      "name": "delete_account",
      "description": "Disconnect a linked bank account, revoking access for the access token.",
      "method": "DELETE",
      "path": "/accounts/{account_id}",
      "input_schema": {
        "type": "object",
        "properties": {
          "account_id": {
            "type": "string",
            "description": "The account id to disconnect"
          }
        },
        "required": [
          "account_id"
        ]
      }
    },
    {
      "name": "get_account_details",
      "description": "Get the account and routing numbers for a linked account (for ACH transfers).",
      "method": "GET",
      "path": "/accounts/{account_id}/details",
      "input_schema": {
        "type": "object",
        "properties": {
          "account_id": {
            "type": "string",
            "description": "The account id from list_accounts"
          }
        },
        "required": [
          "account_id"
        ]
      }
    },
    {
      "name": "get_account_balances",
      "description": "Get the ledger and available balances for a linked account.",
      "method": "GET",
      "path": "/accounts/{account_id}/balances",
      "input_schema": {
        "type": "object",
        "properties": {
          "account_id": {
            "type": "string",
            "description": "The account id from list_accounts"
          }
        },
        "required": [
          "account_id"
        ]
      }
    },
    {
      "name": "list_transactions",
      "description": "List transactions for a linked account, newest first. Returns amount, date, description, running balance, and enriched merchant/category details. Use from_id for cursor pagination.",
      "method": "GET",
      "path": "/accounts/{account_id}/transactions",
      "query_params": [
        "count",
        "from_id"
      ],
      "input_schema": {
        "type": "object",
        "properties": {
          "account_id": {
            "type": "string",
            "description": "The account id from list_accounts"
          },
          "count": {
            "type": "integer",
            "description": "Number of transactions to return"
          },
          "from_id": {
            "type": "string",
            "description": "Return transactions older than this transaction id (cursor pagination)"
          },
          "start_date": {
            "type": "string",
            "description": "Filter transactions on or after this ISO date, for example 2026-01-01."
          },
          "end_date": {
            "type": "string",
            "description": "Filter transactions on or before this ISO date, for example 2026-01-31."
          }
        },
        "required": [
          "account_id"
        ]
      }
    },
    {
      "name": "get_transaction",
      "description": "Get a single transaction for a linked account by transaction id.",
      "method": "GET",
      "path": "/accounts/{account_id}/transactions/{transaction_id}",
      "input_schema": {
        "type": "object",
        "properties": {
          "account_id": {
            "type": "string",
            "description": "The account id from list_accounts"
          },
          "transaction_id": {
            "type": "string",
            "description": "The transaction id from list_transactions"
          }
        },
        "required": [
          "account_id",
          "transaction_id"
        ]
      }
    },
    {
      "name": "get_identity",
      "description": "Get account-holder identity information (names, and where available emails, phone numbers, addresses) for all accounts linked to the access token.",
      "method": "GET",
      "path": "/identity",
      "input_schema": {
        "type": "object",
        "properties": {}
      }
    },
    {
      "name": "create_payee",
      "description": "Create a Teller beta payment payee for an account, currently used for Zelle payments where supported. If Teller returns connect_token, the user must complete MFA in Teller Connect before the payee is saved.",
      "method": "POST",
      "path": "/accounts/{account_id}/payees",
      "headers": {
        "Content-Type": "application/json"
      },
      "input_schema": {
        "type": "object",
        "properties": {
          "account_id": {
            "type": "string",
            "description": "The paying account ID."
          },
          "scheme": {
            "type": "string",
            "enum": [
              "zelle"
            ],
            "description": "Payment scheme. Teller currently documents zelle."
          },
          "address": {
            "type": "string",
            "description": "Payee email address or phone number."
          },
          "name": {
            "type": "string",
            "description": "Payee name."
          },
          "type": {
            "type": "string",
            "enum": [
              "person",
              "business"
            ],
            "description": "Payee type."
          }
        },
        "required": [
          "account_id",
          "scheme",
          "address",
          "name",
          "type"
        ]
      }
    },
    {
      "name": "create_payment",
      "description": "Initiate a beta Zelle payment where supported. First check account.links.payments and supported schemes. Uses JSON payee data and an Idempotency-Key. If the response contains connect_token, payment needs MFA: initialize Teller Connect with connectToken and let the account holder complete it. Do not report success or submit a new payment while the challenge is pending. Check get_payment/list_payments after completion.",
      "method": "POST",
      "path": "/accounts/{account_id}/payments",
      "headers": {
        "Content-Type": "application/json"
      },
      "input_schema": {
        "type": "object",
        "properties": {
          "account_id": {
            "type": "string",
            "description": "The paying account ID."
          },
          "amount": {
            "type": "string",
            "description": "Payment amount as a decimal string, for example 10.48."
          },
          "memo": {
            "type": "string",
            "description": "Optional payment memo."
          },
          "payee": {
            "type": "object",
            "description": "Existing payee: scheme and address. New payee: also provide name and type.",
            "properties": {
              "scheme": {
                "type": "string",
                "enum": [
                  "zelle"
                ],
                "description": "Payment scheme. Teller currently documents zelle."
              },
              "address": {
                "type": "string",
                "description": "Payee email address or phone number."
              },
              "name": {
                "type": "string",
                "description": "Payee name."
              },
              "type": {
                "type": "string",
                "enum": [
                  "person",
                  "business"
                ],
                "description": "Payee type."
              }
            },
            "required": [
              "scheme",
              "address"
            ]
          },
          "idempotency_key": {
            "type": "string",
            "minLength": 1,
            "description": "Stable unique key per payment intent. Persist before sending; reuse for retries of the same payload. Teller retains idempotency behavior for 72 hours."
          }
        },
        "required": [
          "account_id",
          "payee",
          "amount",
          "idempotency_key"
        ]
      },
      "header_params": {
        "idempotency_key": "Idempotency-Key"
      }
    },
    {
      "name": "list_payments",
      "description": "List Teller beta payments initiated through the API for one account.",
      "method": "GET",
      "path": "/accounts/{account_id}/payments",
      "input_schema": {
        "type": "object",
        "properties": {
          "account_id": {
            "type": "string",
            "description": "The account ID."
          }
        },
        "required": [
          "account_id"
        ]
      }
    },
    {
      "name": "get_payment",
      "description": "Get one Teller beta payment by ID.",
      "method": "GET",
      "path": "/accounts/{account_id}/payments/{payment_id}",
      "input_schema": {
        "type": "object",
        "properties": {
          "account_id": {
            "type": "string",
            "description": "The account ID."
          },
          "payment_id": {
            "type": "string",
            "description": "The Teller payment ID."
          }
        },
        "required": [
          "account_id",
          "payment_id"
        ]
      }
    },
    {
      "name": "get_payment_capabilities",
      "method": "OPTIONS",
      "path": "/accounts/{account_id}/payments",
      "description": "Discover supported payment schemes for an eligible account. Check account.links.payments first. Teller currently documents beta Zelle, not general ACH, SEPA or wire support.",
      "input_schema": {
        "type": "object",
        "properties": {
          "account_id": {
            "type": "string",
            "description": ""
          }
        },
        "required": [
          "account_id"
        ]
      }
    }
  ]
}
