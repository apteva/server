.env
.env.*
*.db
*.db-shm
*.db-wal
data/
server
apteva-server
# CLI / core binaries occasionally land here when the local build
# script copies them as siblings during dev — keep the server repo
# free of cross-component build artifacts.
apteva
apteva-core
