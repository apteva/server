Public TrueLayer verification fixtures from https://github.com/TrueLayer/truelayer-signing/tree/main/test-resources (Apache-2.0 OR MIT).
`truelayer-signature.txt` is the upstream webhook-signature.txt; truelayer-jwks.json is the upstream jwks.json. No live credentials are included.
